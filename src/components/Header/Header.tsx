import React from 'react';
import './Header.css';

const Header: React.FC = () => {
  return (
    <header className="header">
      <h1 className="header-title">Nome do Projeto</h1>
      {/* Outros elementos do header */}
    </header>
  );
};

export default Header;
