export const validateEmail = (email: string): boolean => {
    // Implemente a validação do formato de e-mail aqui
  };
  
  export const validatePassword = (password: string): boolean => {
    // Implemente a validação da senha aqui
  };
  